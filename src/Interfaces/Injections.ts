/**
 * Template injection targets.
 * These are variables that should be injected straight into bash scripts.
 */
export type InjectionTargets = 'CONTAINER_VALIDATION_TARGETS' | 'REPO_NAME' | 'COMPOSE_FILE';